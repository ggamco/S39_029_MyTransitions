<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!--DEFINIMOS QUE NUESTRA APP EN LA V21 TENDRA TRANSICIONES-->
        <item name="android:windowContentTransitions">true</item>

        <!--EVITAMOS QUE LAS TRANSICIONES SE SOLAPEN-->
        <item name="android:windowAllowEnterTransitionOverlap">false</item>
        <item name="android:windowAllowReturnTransitionOverlap">false</item>

        <!--PERMITIMOS QUE LAS TRANSICIONES COMPARTIDAS FUNCIONEN-->
        <item name="android:windowSharedElementsUseOverlay">true</item>

        <!--DEFINIMOS LAS TRANSICIONES DE ENTRADA SALIDA-->
        <item name="android:windowEnterTransition">@transition/explode</item>
        <item name="android:windowExitTransition">@transition/explode</item>

        <!--DEFINIMOS LAS TRANSICIONES SALIDA REENTRADA-->
        <item name="android:windowReturnTransition">@transition/fade</item>
        <item name="android:windowReenterTransition">@transition/fade</item>

        <!--DEFINIMOS LAS TRANSICIONES DE ELEMENTOS COMPARTIDOS-->
        <item name="android:windowSharedElementEnterTransition">@transition/change_image_transform</item>
        <item name="android:windowSharedElementExitTransition">@transition/change_image_transform</item>
    </style>

</resources>
